# Make a cube rotate with three js 

## Requirements:

`brew install vite`
`yarn create vite`
Insert fancy project name
`yarn add three @react-three/fiber @react-three/drei`

## Running the code:
Go Inside project folder
`yarn`
`yarn start`

